{
  "name": "htd-official",
  "private": true,
  "type": "module",
  "scripts": {
    "scaffold": "node scripts/scaffold.js",
    "generate": "node scripts/generate-registry.js",
    "build": "node scripts/generate-registry.js && node scripts/build.js"
  },
  "dependencies": {
    "markdown-it": "^14.2.0"
  }
}
